# Record 0003 — First build

Question: what the first `lake build` on the sovereign's machine required.

Disposition: three fix classes, all anticipated in PLAN Phase 0. Elaboration order in
`res_judicata` (split into two `have`s); `cases`-substitution in `sovereign_floor` (the
scrutinee is rewritten in the goal, so `rfl` replaces the consumed equation); and the proof
kernel reduces `String.isPrefixOf` too slowly for `decide`, so `Examples.lean` closes by
`native_decide`. The last widens the trusted base to the Lean compiler for the five example
vectors only; Charter Art. 8 amended to name it, and retiring it is now Phase 1's first
item. All theorems in `Proofs.lean` remain compiler-independent.

Diff: this commit.
